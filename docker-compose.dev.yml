services:
  redis-dev:
    image: redis:7-alpine
    container_name: aid-redis-dev
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_dev_data:/data

volumes:
  redis_dev_data:
